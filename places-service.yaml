spring:
  datasource:
    url: jdbc:postgresql://https://10.10.146.230:${DB_PORT:5432}/places_repo?schema=places_schema
    username: places_repo
    password: places-repo
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    database: postgresql
    show-sql: true
    properties:
      hibernate:
        default_schema: places_schema
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  redis:
    host: 10.10.146.230
    port: 6379
  cache:
    type: redis
    redis:
      time-to-live: 600000
      cache-null-values: false

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:host.docker.internal}:${EUREKA_PORT:8761}/eureka/
    register-with-eureka: true
    fetch-registry: true

flyway:
  locations: classpath:db/migration
  schemas: ${DB_SCHEMA:places_schema}
  baseline-on-migrate: true

